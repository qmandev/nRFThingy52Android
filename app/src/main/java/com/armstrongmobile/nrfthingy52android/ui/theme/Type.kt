package com.armstrongmobile.nrfthingy52android.ui.theme

import androidx.compose.material3.Typography

// Material 3 default type scale (system font — Roboto). The iOS app uses the system SF font
// throughout with no custom type scale, so the platform default is the faithful match (plan Phase 1).
val Typography = Typography()
